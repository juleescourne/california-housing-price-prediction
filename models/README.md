# Generated models

The modeling notebook can export the trained XGBoost model and metadata to this directory.
Generated model artifacts (`.pkl`, `.joblib`, `.json`) are intentionally ignored by Git because they are reproducible from the notebooks and source data.
